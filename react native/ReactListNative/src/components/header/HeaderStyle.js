import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    Header: {
        width: '100%',
        height: 80,
        alignItems: 'center',
        justifyContent: 'center',
    },
    Headertext: {
        fontSize: 24,
        color: '#fff',
        borderBottomWidth: 3,
        borderStyle: 'solid',
        borderColor: '#60a771',
    },
})